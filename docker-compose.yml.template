version: '3.8'

services:
  bot:
    image: glotoff/transcribe-bot:latest
    container_name: my-transcribe-bot
    restart: unless-stopped
    environment:
      - TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
      - OPENAI_API_KEY=your_openai_api_key_here
